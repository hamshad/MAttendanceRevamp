<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools">
    <!-- Location permissions -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- Required for background location tracking (field employees with app minimised) -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <!-- Required to run a foreground service that keeps the GPS stream alive -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <!-- Keeps CPU awake inside the foreground service so Dart timers don't pause -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Lets the app request exclusion from battery optimization (Doze mode) -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <!-- Allows BootReceiver to restart field tracking after a device reboot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <!-- Required for geofence_service to detect when the user is moving -->
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
    <!-- WiFi state access for auto-punch background worker -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:minSdkVersion="33" android:name="android.permission.NEARBY_WIFI_DEVICES" />
    <!-- Wake lock so GeofenceAlarmReceiver can start the service reliably -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Auto-granted normal permission for exact alarms (API 31+), no user prompt -->
    <uses-permission android:minSdkVersion="31" android:name="android.permission.USE_EXACT_ALARM" />
    <!-- Required to post foreground service notifications (Android 13+ runtime permission) -->
    <uses-permission android:minSdkVersion="33" android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:label="mAttendance"
        android:name=".MainApplication"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Required by image_cropper (uCrop) -->
        <activity
            android:name="com.yalantis.ucrop.UCropActivity"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar"/>
        <!-- Geolocator foreground service — keeps GPS stream alive when app is minimised -->
        <service
            android:name="com.baseflow.geolocator.ForegroundLocationService"
            android:foregroundServiceType="location"
            android:exported="false" />
        <!-- flutter_foreground_task — keeps the geofence service alive in background -->
        <service 
            android:name="com.pravera.flutter_foreground_task.service.ForegroundTaskService" 
            android:foregroundServiceType="location" 
            android:stopWithTask="true" />
        <!-- flutter_background_service — keeps the Dart isolate alive on OEM devices -->
        <service
            android:name="id.flutter.flutter_background_service.BackgroundService"
            android:foregroundServiceType="location"
            android:exported="false"
            tools:replace="android:exported,android:foregroundServiceType" />

        <!-- Restarts field tracking after device reboot if it was active before shutdown -->
        <receiver
            android:name=".BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>
        <!-- AlarmManager-based shift-start alarm (more reliable than WorkManager on OEM ROMs) -->
        <receiver
            android:name=".GeofenceAlarmReceiver"
            android:exported="false" />
        <!-- Periodic containment check (15 min, Doze-tolerant): guarantees
             auto punch-out/IN even when the OS misses geofence transitions
             while backgrounded.  Self-perpetuating; enqueues headless
             WorkManager work (no foreground service). -->
        <receiver
            android:name=".ContainmentAlarmReceiver"
            android:exported="false" />

        <!-- Used by plugin: native_geofence — OS-native geofencing events.
             BroadcastReceiver: receives GeofencingClient transitions (spawns
             the plugin's WorkManager isolate → Dart geofence callback).
             RebootBroadcastReceiver: re-registers geofences after reboot.
             ForegroundService: hosts the callback while doing punch work. -->
        <receiver android:name="com.chunkytofustudios.native_geofence.receivers.NativeGeofenceBroadcastReceiver"
                  android:exported="true"/>
        <receiver android:name="com.chunkytofustudios.native_geofence.receivers.NativeGeofenceRebootBroadcastReceiver"
                  android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"></action>
            </intent-filter>
        </receiver>
        <service android:name="com.chunkytofustudios.native_geofence.NativeGeofenceForegroundService"
                  android:permission="android.permission.BIND_JOB_SERVICE" android:exported="true"/>

        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
